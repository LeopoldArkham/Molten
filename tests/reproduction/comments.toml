# A comment at the top
val1 = true
# A comment followed by whitespace

# A comment followed and preceded by whitespace

val2 = false #A comment on a KeyValue pair
val3 = true#NOSPACES

# An EOF comment